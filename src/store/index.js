import Vue from 'vue'
import Vuex from 'vuex'
import dialogs from './modules/dialogs';

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
      dialogs
  }
})
